"""
"SAIL Mines Production & Despatch Performance" — placeholder page from
Report_format/index.txt's Index rewrite (item 7, right after "Production
main Items"). index.txt marks this section's content "(empty page contents
awaited)" — no mines production/despatch data source exists anywhere in
this app yet, so this is a title-only placeholder page (see main.py's
SAIL_MINES_PAGE_ID) until that data source is extracted/wired up.
"""


def generate_sail_mines() -> dict:
    return {
        "title": "SAIL Mines Production & Despatch Performance",
        "available": False,
    }
