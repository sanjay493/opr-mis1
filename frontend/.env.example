# Copy this file to .env.local and adjust for your machine.
# Base URL of the FastAPI backend as reachable FROM THE BROWSER.
# Use the dev machine's LAN IP (not 127.0.0.1) if other computers
# on the network will access the app.
NEXT_PUBLIC_API_URL=http://10.135.5.15:8082
